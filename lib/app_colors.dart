import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF007AFF);
  static const Color accent = Color(0xFFFF3B30);
  static const Color background = Color(0xFFF8F8F8);
  static const Color text = Color(0xFF333333);
  static const Color foodCategory = Color(
    0xFFE57373,
  ); // Merah lembut untuk Makanan
  static const Color drinkCategory = Color(
    0xFF64B5F6,
  ); // Biru lembut untuk Minuman
  static const Color electronicCategory = Color(
    0xFF81C784,
  ); // Hijau lembut untuk Elektronik
}
